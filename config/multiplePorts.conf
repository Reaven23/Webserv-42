server {
    listen 8080;
    root ./site1;
    index index.html;

    location / {
        methods GET;
    }
}

server {
    listen 8081;
    root ./site2;
    index index.html;

    location / {
        methods GET;
    }
}